<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "org/apache/ibatis/builder/xml/mybatis-3-mapper.dtd">
<mapper namespace="dao.ITripDAO">
    <select id="getEntityById" resultMap="tripResultMap">
        <!--implement-->
    </select>
    <insert id="createEntity" useGeneratedKeys="true" keyProperty="id" parameterType="travelAgency.trip.Trip">
        <!--implement-->
    </insert>
    <update id="updateEntity" parameterType="travelAgency.trip.Trip">
        <!--implement-->
    </update>
    <delete id="removeById" parameterType="int">
        <!--implement-->
    </delete>
    <resultMap id="tripResultMap" type="travelAgency.trip.Trip" autoMapping="false">
        <id column="idTrip" property="id"/>
        <result column="airlineName" property="name"/>
        <!-- <collection property="employees"
                     resultMap="com.solvd.app.dao.IEmployeeDAO.employeeResultMap"/>-->
    </resultMap>
</mapper>
